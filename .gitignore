# Build artifacts
build/
pintool/obj-*/
ali/pintool/obj-*/
*.o
*.so

# Python virtualenv
.venv/

# Logs (always transient)
logs/

# dealII run-time output artifacts (also dropped into ./artifacts/ to keep root clean)
*.gmv
*.eps
/artifacts/

# NFS lockfiles
.nfs*

# Transient root-level smoke outputs (real deliverables live in ali/results/)
/smoke_test.csv
/smoke_verify.csv

# Course homework — kept locally for convenience but not part of this project
/cs146-psets/
